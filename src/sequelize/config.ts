const config = {
    environment: 'dev',
    database: {
        dbName: 'sequelizeTest',
        host: 'localhost',
        port: 3306,
        username: 'root',
        password: 'mysql1024'    // 需要为字符串类型
    }
}

export { config };